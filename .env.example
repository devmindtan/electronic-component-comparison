VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_MINIO_URL=
VITE_MINIO_BUCKET=

# Public upload mode: app uploads directly to
# ${VITE_MINIO_URL}/${VITE_MINIO_BUCKET}/<object-key>
# Ensure bucket policy + CORS allow browser PUT requests.